import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontSize: 24,
    fontWeight: 'bold',
    color: "#f1c40f",
    marginBottom: 20,
    textTransform: "uppercase",
    textAlign: "center",
  }
});
